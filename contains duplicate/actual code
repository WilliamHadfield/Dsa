
fn main() {
  let mut number: Vec<i32> = Vec::new();
  number.push(1);
number.push(5);
number.push(4);
number.push(2);
number.push(3);

println!("{}", has_duplicate(number));

let mut number2: Vec<i32> = Vec::new();
  number2.push(1);
number2.push(3);
number2.push(4);
number2.push(3);
number2.push(3);

println!("{}", has_duplicate(number2));
}
use std::collections::HashSet;
pub fn has_duplicate(nums : Vec<i32>) -> bool {
   if nums.is_empty() {
    return false // quick check for speed, essentially saves time if there nothing in the cotainer and quickly return false rather than iterating and coming to the same conclusion.
   }
   let mut nums = nums; // creates a mutable container for the nums vector
    nums.sort_unstable(); // slightly quicker than .sort() because it doesnt have allocation onto a buffer on the heap making it slightly quicker doesnt gurantee equivalent values have the same order.
    let mut prev = nums[0]; // let the index of the first number in the nums container and bind it to a mutable prev variable
    for i in 1..nums.len() { // for loop
        if nums[i] == prev { 
            return true;
        }
    prev = nums[i] // essentially whats its doing is, its cycle 0, 1 1,2 each for loop iteration, essentially the logic goes: prev = index 0, then for 1..length, is the current index = to the previous one? return true if not?
    // restart the loop and bind the index iterated over to previous so eg it goes from 0-> 1.
        }

        return false // then if after cycling through the whole series and still no matching pair then return false.



    
}
